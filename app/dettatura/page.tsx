import { DettaturaView } from "@/components/dettatura/DettaturaView";

/**
 * Dettatura — page vii · dattilografia.
 * Typing trainer for paragraph cards. See DettaturaView.
 *
 * (The original flash-then-write "a memoria" mode was removed on 2026-05-03;
 * only the typing-trainer half of the page remains.)
 */
export default function DettaturaPage() {
  return <DettaturaView />;
}
